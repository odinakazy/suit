import AuthLayout from "../auth-layout";
import OtpForm from "./components/otp-form";

const Otp = () => {
  return (
    <AuthLayout>
      <OtpForm />
    </AuthLayout>
  );
};

export default Otp;
